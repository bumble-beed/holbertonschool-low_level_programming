#include <main.h>

/**
 * main - prints the alphabet, followed by a new line using prototype and putchar
 *
 * Return: 0
 */
void print_alphabet(void);
{
	char lower_case = 'a';
	
	while (lower_case <= 'z')
	{
		_putchar(c);
		lower_case++;
	}
	_putchar('\n');
}

